const mainContainer = {
  w: '100%',
  h: '100%',
  justifyContent: 'center',
  alignItems: 'center',
};

const buttonStyle = {
  mt: 6,
  bgColor: 'green.400',
  color: 'white',
  _hover: { bgColor: 'green.500' },
};

const inputUsername = {
  backgroundColor: 'white',
  mt: 10,
};

const inputRoom = {
  mt: 4,
  backgroundColor: 'white',
};

const styles = {
  mainContainer,
  buttonStyle,
  inputUsername,
  inputRoom,
};

export default styles;
