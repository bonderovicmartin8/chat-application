const messageContainer = {
  maxH: '15vh',
  maxW: '20vw',
  p: 2,
  borderRadius: 8,
  fontSize: 'xl',
};

const messageWrapper = {
  w: '100%',
  paddingInline: 15,
  mt: 2,
};

const styles = {
  messageContainer,
  messageWrapper,
};

export default styles;
