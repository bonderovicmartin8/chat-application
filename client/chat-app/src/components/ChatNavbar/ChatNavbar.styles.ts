const navbarContainer = {
  w: '100%',
  flex: '0.2',
  p: '4',
  alignItems: 'center',
};

const navbarHeader = {
  fontSize: '3xl',
  ml: '10',
};

const styles = {
  navbarContainer,
  navbarHeader,
};

export default styles;
