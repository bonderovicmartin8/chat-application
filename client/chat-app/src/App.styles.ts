const messageWrapper = {
  flex: '2.5',
  justifyContent: 'center',
  alignItems: 'center',
};

const messageContainer = {
  w: '100%',
  h: '100%',
  flex: '3',
};

const activeUsersContainer = {
  flex: '1',
  w: '100%',
  h: '100%',
  bgColor: 'gray.50',
};

const headingForActiveUsers = {
  fontSize: '3xl',
  p: 4,
};

const styles = {
  messageWrapper,
  messageContainer,
  activeUsersContainer,
  headingForActiveUsers,
};

export default styles;
